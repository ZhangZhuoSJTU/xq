# Course-side exclusions: tests that are environment-sensitive outside the
# benchmark containers. Format: <branch>/<classname>.<testname>

# The golden captures one specific interleaving of partial formatted output
# and the syntax-error message on stdout. The two are flushed independently,
# and the interleaving order differs between environments, so identical bytes
# arrive in a different order locally.
0ce13a636ff1/tests.test_io_modes.test_malformed_xml_error
